// Interface for adjustable devices
namespace SMARTHOMESYSTEM
{
    
    // Adjusts a setting level (e.g., brightness, volume, or cooling level)
    interface IAdjustable
    {
        void AdjustLevel(int level);
    }


}
